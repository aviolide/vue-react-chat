const messagesMethods = {
  'message.add': 'message.add',
  'message.get': 'message.get'
};

const membersMethods = {
  'member.add': 'member.add',
  'member.get': 'member.get'
};

export const messages = {
  add: messagesMethods['message.add'],
  get: messagesMethods['message.get']
};

export const members = {
  add: membersMethods['member.add'],
  get: membersMethods['member.get']
};
